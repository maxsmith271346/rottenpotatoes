<p style="color: green"><%= notice %></p>

<%= render @movie %>

<div>
  <%= link_to "Edit this movie", edit_movie_path(@movie, column: params[:column], order: params[:order]) %> |
  <%= link_to "Back to movies", movies_path(column: params[:column], order: params[:order]) %>

  <%= button_to "Destroy this movie", movie_path(@movie, {column: params[:column], order: params[:order]}), method: :delete %>
</div>
